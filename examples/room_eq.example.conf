Filter Settings file

Room EQ V5.20
Dated: 2026-07-12

Notes:
  ★ 이 파일은 예시 템플릿입니다 — 실제 측정 결과가 아닙니다.
  ★ 반드시 본인의 스피커+룸을 REW 로 실측한 필터로 교체하세요.
  ★ 사용법: 이 파일을 ~/.config/roomtune/room_eq.conf 로 복사하거나
     ROOMTUNE_EQ_FILE 환경변수로 경로를 지정한 뒤 플레이어를 실행하고
     View > Room EQ (test) 를 켭니다.
  ★ 포맷 = REW "Filter Settings as text" (= EQ APO). 지원 타입: PK, LS, HS, LP, HP.
  ★ 엔진 규칙: 부스트는 +6 dB 로 자동 클램프, 컷은 제한 없음.
     접근성 채널(HI=6, VI=7)은 보정에서 제외됨. fs 는 DCP 표준 48 kHz 가정.

Equaliser: Generic

# Preamp: 부스트로 인한 클리핑 방지용 헤드룸. 통상 음수(감쇠). RoomTune 은 +값이면 0 으로 상한.
Preamp: -4.0 dB

# --- 예시: 소형 룸의 전형적 저역 모드 컷 + X-curve 성향 고역 롤오프 ---
# (수치는 예시일 뿐 — 실측으로 반드시 대체)
Filter  1: ON  PK       Fc    42.0 Hz  Gain  -6.0 dB  Q  5.000
Filter  2: ON  PK       Fc    68.0 Hz  Gain  -4.5 dB  Q  4.000
Filter  3: ON  PK       Fc   125.0 Hz  Gain  -3.0 dB  Q  3.000
Filter  4: ON  PK       Fc   240.0 Hz  Gain   2.5 dB  Q  2.000
Filter  5: ON  LS       Fc    50.0 Hz  Gain   2.0 dB  Q  0.700
Filter  6: ON  HS       Fc  8000.0 Hz  Gain  -3.0 dB  Q  0.700
Filter  7: OFF None
